# DEPLOYMENT dependencies — exactly what the live Streamlit app needs.
# Streamlit Community Cloud installs this file (repo root) when building the
# app, so it is kept lean: fast free-tier builds, low RAM footprint.
#
# Full development environment (EDA, monitoring, API, tests): requirements-dev.txt
#
# Do NOT pin numpy to 1.26 here. Community Cloud currently often builds with
# Python 3.14, which has no numpy==1.26 wheels; a failed numpy install then
# leaves joblib/sklearn missing and the app crashes on import. Flexible lower
# bounds let the resolver pick wheels that exist for the Cloud Python version.
# (evidently still needs numpy<2 in the *dev* environment only.)

numpy>=1.26.4
pandas>=2.2.3
scikit-learn>=1.5.2
lightgbm>=4.5.0
joblib>=1.4.2
streamlit>=1.40.2
